#!/usr/bin/env python
#coding:utf-8
#注：此库内容全部原创，请匆在不得到作者同意的情况下以任何形式转载，谢谢支持。

#质数查询
def prime_check(x):
    try:
        x%2=fn
        if fn==0:
            return False
        elif fn==1:
            (x+1)/2=sn
        else:
            return "Error"
        i=2
        while i<sn:
            x//sn=test
            if test==True:
                return False
            elif test==False:
                i+=1
            else:
                return "Error"
        return True
    except Exception:
        return "Error"

    
 #最大公约数
def 
